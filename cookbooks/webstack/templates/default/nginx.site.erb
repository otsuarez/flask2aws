server {
  listen 80;
  set $app_name <%= node[:webstack][:appname] %>; 

  access_log  /var/log/nginx/<%= node[:webstack][:appname] %>.access.log;

  location  / {
    include uwsgi_params;
    uwsgi_pass unix:/tmp/$app_name.sock;
  }
}
